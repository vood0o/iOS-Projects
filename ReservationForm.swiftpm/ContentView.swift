import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            ReservationForm()
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundColor(.accentColor)
            Text("Welcome!")
        }
    }
}
